define([], function () {
    var configLocal = {};

    configLocal.api = {
        name: 'CHoRUS Engineer',
        url: '/webapi/'
    };

    configLocal.userAuthenticationEnabled = false;

    return configLocal;
});
