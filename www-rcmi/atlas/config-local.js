define([], function () {
    var configLocal = {};

    configLocal.api = {
        name: 'CHoRUS',
        url: '/webapi/'
    };

    configLocal.userAuthenticationEnabled = false;

    return configLocal;
});
